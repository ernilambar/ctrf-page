# ctrf-page

Converts a CTRF JSON report into a human-readable HTML file and opens it in the browser.

## Usage

```bash
node parse.js <path-to-report.json>
```

The HTML file is written next to the JSON file. No dependencies required.
